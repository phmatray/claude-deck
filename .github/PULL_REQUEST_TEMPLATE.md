## Summary

<!-- What this PR changes and why. -->

## Checklist

- [ ] A `check-*` script covers the new logic, and it is named on a `run:` line in `.github/workflows/ci.yml`
- [ ] `corepack pnpm build && corepack pnpm sd:validate` in `stream-deck/`, and every `check-*` script, are green
- [ ] Docs updated (`README.md`, `CLAUDE.md`, `docs/`) if behaviour or usage changed
- [ ] PR title follows Conventional Commits — it becomes the squash subject

## Verified on hardware

<!-- Which deck, which key, what you pressed, what happened. "Not tested on hardware" is a fine answer. -->
